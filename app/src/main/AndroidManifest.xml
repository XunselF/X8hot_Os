<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xunself.x8hot_os">

    <!-- 权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppNoActionBarTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".BoxAboutActivity" />
        <activity android:name=".AddBoxsActivity" />
        <activity
            android:name=".WorkOrderActivity"
            android:label="提交工单"
            android:theme="@style/AlertDialogNoActionBar" />
        <activity android:name=".RecordActivity" />
        <activity
            android:name=".DeleteActivity"
            android:label="删除！"
            android:theme="@style/AlertDialogNoActionBar" />
        <activity
            android:name=".WorkOrderDeleteActivity"
            android:label="删除！"
            android:theme="@style/AlertDialogNoActionBar" />
        <activity android:name=".WorkOrderUpdateActivity"
            android:label="修改"
            android:theme="@style/AlertDialogNoActionBar"></activity>
    </application>

</manifest>